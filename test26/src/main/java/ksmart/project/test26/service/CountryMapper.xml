<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ksmart.project.test26.service.CountryMapper">
	<!-- 셀렉트 쿼리 -->
	<select id="selectCountryList" resultType="ksmart.project.test26.service.Country">
		SELECT
		country_id as countryId
		,country_name as countryName
		FROM test.country
	</select>
	
	<!-- 인설트 쿼리 -->
	<insert id="insertCountry" parameterType="ksmart.project.test26.service.Country">
	   	INSERT INTO test.contry ()country_name)
        VALUES (#{countryId})       
    </insert>
    
	<!-- 업데이트 쿼리 -->
	<!-- <update id="updateCountry" parameterType="ksmart.project.test26.service.Country">
		UPDATE set
			country_name= #{countryName}
			from test.country
			where country_id = #{countryId}
	</update> -->
	
	<!-- 딜레이트 쿼리 -->
	<!-- <delete id="deleteCountry" parameterType="cafe.jjdev.board.service.Board">
        DELETE FROM test.country
        	WHERE country_id = #{countryId} 
        AND country_name = #{countryName}
    </delete> -->
</mapper>